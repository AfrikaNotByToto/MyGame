import styled from 'styled-components';
import { createButton, ButtonRoot } from '@salutejs/plasma-core';
import { applySizes } from './Button.mixins';
import { buttonViews } from './Button.props';
var StyledButtonRoot = /*#__PURE__*/styled(ButtonRoot).withConfig({
  componentId: "plasma-web__sc-1wja093-0"
})(["", " ", ""], applySizes, function (_ref) {
  var view = _ref.view;
  return buttonViews[view];
});
/**
 * Кнопка.
 * Поддерживает несколько режимов отображения (`view`) и размеров (`size`).
 */

export var Button = /*#__PURE__*/createButton(StyledButtonRoot);
Button.defaultProps = {
  size: 'm',
  view: 'secondary',
  pin: 'square-square'
};